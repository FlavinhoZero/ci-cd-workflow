name: Hello Reusable

on:
  workflow_call:
    inputs:
      name:
        description: 'Name to greet'
        required: false
        default: 'world'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello ${{ inputs.name }}"